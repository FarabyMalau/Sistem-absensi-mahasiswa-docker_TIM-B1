<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Mahasiswa - Sistem Absensi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root { --primary: #4F46E5; --secondary: #10B981; }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .navbar-custom {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 15px 25px;
            margin-bottom: 30px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
        }
        .navbar-brand {
            font-weight: 800;
            font-size: 1.5rem;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .card-custom {
            background: white;
            border-radius: 20px;
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.12);
            border: none;
            overflow: hidden;
            margin-bottom: 30px;
            animation: fadeInUp 0.6s ease;
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .card-header-custom {
            background: linear-gradient(135deg, var(--primary), #6366F1);
            color: white;
            padding: 25px 30px;
            border: none;
        }
        .card-header-custom h4 { margin: 0; font-weight: 700; font-size: 1.6rem; }
        .form-control-custom {
            border-radius: 12px;
            border: 2px solid #E5E7EB;
            padding: 14px 18px;
            transition: all 0.3s ease;
            font-size: 1rem;
        }
        .form-control-custom:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1);
        }
        .btn-custom {
            border-radius: 12px;
            padding: 12px 24px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            font-size: 0.95rem;
        }
        .btn-primary-custom {
            background: linear-gradient(135deg, var(--primary), #6366F1);
            color: white;
        }
        .btn-primary-custom:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(79, 70, 229, 0.35);
        }
        .btn-warning-custom {
            background: linear-gradient(135deg, #F59E0B, #FBBF24);
            color: white;
        }
        .btn-danger-custom {
            background: linear-gradient(135deg, #EF4444, #F87171);
            color: white;
        }
        .btn-secondary-custom {
            background: #6B7280;
            color: white;
        }
        .table-custom {
            border-radius: 12px;
            overflow: hidden;
        }
        .table-custom thead {
            background: linear-gradient(135deg, #374151, #1F2937);
            color: white;
        }
        .table-custom thead th {
            padding: 18px 15px;
            font-weight: 600;
            border: none;
        }
        .table-custom tbody td {
            padding: 16px 15px;
            vertical-align: middle;
            border-bottom: 1px solid #F3F4F6;
        }
        .table-custom tbody tr {
            transition: all 0.2s ease;
        }
        .table-custom tbody tr:hover {
            background: #F9FAFB;
        }
        .badge-custom {
            padding: 10px 18px;
            border-radius: 25px;
            font-weight: 600;
            font-size: 0.9rem;
        }
        .badge-info-custom { background: #DBEAFE; color: #1E40AF; }
        .stats-badge {
            background: linear-gradient(135deg, var(--secondary), #34D399);
            color: white;
            padding: 15px 25px;
            border-radius: 15px;
            display: inline-block;
            font-weight: 700;
            font-size: 1.1rem;
            margin-bottom: 20px;
        }
        .modal-content {
            border-radius: 20px;
            border: none;
        }
        .modal-header {
            background: linear-gradient(135deg, var(--primary), #6366F1);
            color: white;
            border-radius: 20px 20px 0 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Navbar -->
        <nav class="navbar-custom">
            <div class="d-flex justify-content-between align-items-center w-100">
                <span class="navbar-brand">
                    <i class="fas fa-graduation-cap"></i> Sistem Absensi
                </span>
                <div>
                    <a href="/" class="btn btn-secondary-custom me-2">
                        <i class="fas fa-home"></i> Home
                    </a>
                    <a href="/absensi" class="btn btn-primary-custom">
                        <i class="fas fa-clipboard-check"></i> Absensi
                    </a>
                </div>
            </div>
        </nav>

        <!-- Form Tambah Mahasiswa -->
        <div class="card-custom">
            <div class="card-header-custom">
                <h4><i class="fas fa-user-plus me-2"></i>Tambah Mahasiswa Baru</h4>
            </div>
            <div class="card-body p-4">
                <form action="/mahasiswa/add" method="POST">
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">
                                <i class="fas fa-id-card me-2" style="color: var(--primary);"></i>NIM
                            </label>
                            <input type="text" name="nim" class="form-control form-control-custom" 
                                   placeholder="Contoh: 20240140001" required>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">
                                <i class="fas fa-user me-2" style="color: var(--primary);"></i>Nama Lengkap
                            </label>
                            <input type="text" name="nama" class="form-control form-control-custom" 
                                   placeholder="Contoh: Ahmad Rizki" required>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">
                                <i class="fas fa-chalkboard me-2" style="color: var(--primary);"></i>Kelas
                            </label>
                            <input type="text" name="kelas" class="form-control form-control-custom" 
                                   placeholder="Contoh: TI-A" required>
                        </div>
                    </div>
                    <div class="mt-4">
                        <button type="submit" class="btn btn-primary-custom">
                            <i class="fas fa-save me-2"></i>Simpan Data
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Tabel Data Mahasiswa -->
        <div class="card-custom">
            <div class="card-header-custom">
                <div class="d-flex justify-content-between align-items-center">
                    <h4><i class="fas fa-users me-2"></i>Daftar Mahasiswa</h4>
                    <span class="stats-badge">
                        <i class="fas fa-database me-2"></i><%= mahasiswa.length %> Mahasiswa
                    </span>
                </div>
            </div>
            <div class="card-body p-4">
                <% if (mahasiswa.length > 0) { %>
                    <div class="table-responsive">
                        <table class="table table-custom">
                            <thead>
                                <tr>
                                    <th width="8%">No</th>
                                    <th width="18%">NIM</th>
                                    <th width="30%">Nama Lengkap</th>
                                    <th width="18%">Kelas</th>
                                    <th width="26%">Aksi</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% mahasiswa.forEach((m, index) => { %>
                                    <tr>
                                        <td><strong>#<%= index + 1 %></strong></td>
                                        <td><span class="badge badge-custom badge-info-custom"><%= m.nim %></span></td>
                                        <td><i class="fas fa-user-circle me-2" style="color: var(--primary);"></i><%= m.nama %></td>
                                        <td><span class="badge badge-custom badge-info-custom"><%= m.kelas %></span></td>
                                        <td>
                                            <button class="btn btn-warning-custom btn-sm me-1" 
                                                    data-bs-toggle="modal" 
                                                    data-bs-target="#editModal<%= m.id %>">
                                                <i class="fas fa-edit"></i> Edit
                                            </button>
                                            <button class="btn btn-danger-custom btn-sm" 
                                                    data-bs-toggle="modal" 
                                                    data-bs-target="#deleteModal<%= m.id %>">
                                                <i class="fas fa-trash"></i> Hapus
                                            </button>
                                        </td>
                                    </tr>

                                    <!-- Modal Edit -->
                                    <div class="modal fade" id="editModal<%= m.id %>" tabindex="-1">
                                        <div class="modal-dialog">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title">
                                                        <i class="fas fa-edit me-2"></i>Edit Data Mahasiswa
                                                    </h5>
                                                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                                </div>
                                                <form action="/mahasiswa/update/<%= m.id %>" method="POST">
                                                    <div class="modal-body">
                                                        <div class="mb-3">
                                                            <label class="form-label fw-semibold">NIM</label>
                                                            <input type="text" name="nim" class="form-control form-control-custom" 
                                                                   value="<%= m.nim %>" required>
                                                        </div>
                                                        <div class="mb-3">
                                                            <label class="form-label fw-semibold">Nama Lengkap</label>
                                                            <input type="text" name="nama" class="form-control form-control-custom" 
                                                                   value="<%= m.nama %>" required>
                                                        </div>
                                                        <div class="mb-3">
                                                            <label class="form-label fw-semibold">Kelas</label>
                                                            <input type="text" name="kelas" class="form-control form-control-custom" 
                                                                   value="<%= m.kelas %>" required>
                                                        </div>
                                                    </div>
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-secondary-custom" data-bs-dismiss="modal">
                                                            <i class="fas fa-times me-2"></i>Batal
                                                        </button>
                                                        <button type="submit" class="btn btn-primary-custom">
                                                            <i class="fas fa-save me-2"></i>Simpan Perubahan
                                                        </button>
                                                    </div>
                                                </form>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Modal Delete -->
                                    <div class="modal fade" id="deleteModal<%= m.id %>" tabindex="-1">
                                        <div class="modal-dialog">
                                            <div class="modal-content">
                                                <div class="modal-header bg-danger">
                                                    <h5 class="modal-title text-white">
                                                        <i class="fas fa-exclamation-triangle me-2"></i>Konfirmasi Hapus
                                                    </h5>
                                                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                                </div>
                                                <div class="modal-body">
                                                    <p>Apakah Anda yakin ingin menghapus data mahasiswa:</p>
                                                    <div class="alert alert-warning">
                                                        <strong><%= m.nama %></strong> (<%= m.nim %>)
                                                    </div>
                                                    <p class="text-danger"><i class="fas fa-exclamation-circle"></i> Data yang dihapus tidak dapat dikembalikan!</p>
                                                </div>
                                                <form action="/mahasiswa/delete/<%= m.id %>" method="POST">
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-secondary-custom" data-bs-dismiss="modal">
                                                            <i class="fas fa-times me-2"></i>Batal
                                                        </button>
                                                        <button type="submit" class="btn btn-danger-custom">
                                                            <i class="fas fa-trash me-2"></i>Ya, Hapus
                                                        </button>
                                                    </div>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                <% } else { %>
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <strong>Belum ada data mahasiswa.</strong> Silakan tambah data menggunakan form di atas.
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
